needs = (what) ->
  if typeof what is 'string' then what = [what]

  (req, res, next) ->
    for item in what
      unless item of req.body
        return res.error 'Required POST data left undefined!'
    next()

module.exports = needs