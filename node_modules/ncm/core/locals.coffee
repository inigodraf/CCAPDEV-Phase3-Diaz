g = require './../global'

locals = (app) ->
  (req, res, next) ->
    app.locals.headers = g.storage.data.headers.all()
    app.locals.links = g.storage.data.links.all()
    next()

module.exports = locals