extends layout

block content
  p
    a(href='user/create') New User
  table
    tr
      td
        h2 User
      td
        h2 Action
    each user in users
      tr
        td= user.username
        td
          a(href='/user/#{user.id}') Edit
        td
          a(href='/user/remove/#{user.id}') Remove