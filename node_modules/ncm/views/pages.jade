extends layout

block content
  if pages
    p
      a(href='page/create') New Page
    table
      tr
        td
          h2 Title
        td
          h2 Action
      each page in pages
        tr
          td= page.title
          td
            a(href='/page/edit/#{page.id}') Edit
          td
            a(href='/page/remove/#{page.id}') Remove
  else
    h2 No pages? What about
    a(href='page/create') creating one! 
    | (Please note that you are required to add a "Home" page as your index!)