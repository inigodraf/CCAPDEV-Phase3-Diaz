extends layout

block content
  p NCM supports markdown for page formatting. Here some information on its 
    a(href='http://daringfireball.net/projects/markdown/syntax') Syntax
  form(action='/page/edit/#{id}', method='post')
    textarea(name='content').
      #{content}
    input(type='submit', value='Submit')