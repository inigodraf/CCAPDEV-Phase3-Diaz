extends layout

block content
  p Uploaded files are stored in the /public/uploads folder.
    form(action='/upload', enctype='multipart/form-data', method='post')
      input(type='file', name='file')
      input(type='submit', value='Submit')