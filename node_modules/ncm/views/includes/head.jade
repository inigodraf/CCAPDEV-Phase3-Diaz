each header in locals.headers
  if header.name
    <#{header.tag} name='#{header.name}' content='#{header.content}' />
  else
    <#{header.tag}>#{header.content}</#{header.tag}>

link(rel='stylesheet', href='/stylesheets/style.css')